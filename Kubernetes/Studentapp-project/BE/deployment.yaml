apiVersion: apps/v1
kind: Deployment
metadata:
  name: studentapp
spec:
  selector:
    matchLabels:
      app: studentapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: <Image>
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: <Port>
